const WHEEL_SCROLL_THRESHOLD = 50 // 휠 스크롤 임계값 (픽셀 단위)
const WHEEL_TIMEOUT = 500 // 휠 이벤트 후 스크롤 잠금 해제 시간 (밀리초)
const TOUCH_SCROLL_THRESHOLD = 130 // 터치 스와이프 임계값 (픽셀 단위)
const TOUCH_TIMEOUT = 300 // 터치 이벤트 후 스크롤 잠금 해제 시간 (밀리초)

export { WHEEL_SCROLL_THRESHOLD, WHEEL_TIMEOUT, TOUCH_SCROLL_THRESHOLD, TOUCH_TIMEOUT }
